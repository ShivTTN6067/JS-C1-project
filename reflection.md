# Reflection

<!-- TEMPLATE: This is your personal reflection. Replace the prompts below with
     your own honest answers before submitting. The notes in italics point to
     evidence in the repo you can draw on. -->

## What I Built

_<In your own words: a full-stack Support Ticket Management System — React + TS
frontend, Express + TS API, SQLite via Prisma — with an enforced status state
machine, search/filter, validation, and tests. See `README.md`.>_

## How I Used AI (across the lifecycle)

_<Describe how you used Cursor for requirement analysis, planning, code
generation, testing, debugging, and review. Evidence: `tool-workflow.md`,
`ai-prompts/`.>_

## What AI Helped With Most

_<e.g. generating boilerplate quickly, enumerating the state-machine transition
matrix and test cases, explaining tooling errors.>_

## What AI Got Wrong

_<e.g. the initial supertest-based test setup that failed under Vitest; a tsconfig
rootDir conflict; the untyped `import.meta.env`. See `debugging-notes.md`.>_

## How I Validated AI Output

_<e.g. ran `tsc` + both test suites after each change, manual curl smoke tests,
hand-checked the transition table against the brief. See `test-results.md`.>_

## What I Would Improve Next

_<e.g. add auth, priority/assignee filters, pagination, detail-page component
tests, E2E tests, OpenAPI docs, and CI. See `pr-description.md` future work.>_

## Reusable Workflow (prompts, rules, specs, templates)

_<What you would carry into a real project: persistent project context + spec +
rules, spec-before-generation, test-gated acceptance. See
`tool-specific/cursor-workflow/`.>_
