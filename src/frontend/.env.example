# Frontend environment variables
# Copy to .env if you need to override the API base URL.
# By default the Vite dev server proxies /api to http://localhost:4000
# (see vite.config.ts), so this is usually not required in development.
VITE_API_BASE_URL="/api"
